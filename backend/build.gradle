plugins {
    id 'java'
    id 'org.springframework.boot' version '3.5.6'
    id 'io.spring.dependency-management' version '1.1.7'
}

group = 'com.multi'
version = '0.0.1-SNAPSHOT'
description = 'multi home pages'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

repositories {
    mavenCentral()
}

dependencies {
    // 📌 Spring Boot 핵심 스타터
    implementation 'org.springframework.boot:spring-boot-starter'

    // 📌 웹 개발을 위한 필수 의존성: 내장 Tomcat, Spring MVC 포함
    implementation 'org.springframework.boot:spring-boot-starter-web'

    // 📌 데이터베이스 연결에 필요한 의존성: JPA (Java Persistence API)
    // 객체를 관계형 데이터베이스에 매핑해주는 기술
    // 아직 데이터베이스를 사용하지 않더라도 미리 추가해두면 좋습니다.
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'

    // 📌 템플릿 엔진 의존성: Thymeleaf
    // HTML 파일을 동적으로 렌더링할 때 사용
    // 프론트엔드와 백엔드를 통합할 때 필요합니다.
    implementation 'org.springframework.boot:spring-boot-starter-thymeleaf'

    // 📌 Lombok: Getter, Setter, 생성자 등을 자동으로 만들어주는 라이브러리
    // 코드를 간결하게 만들어 개발 생산성을 높여줍니다.
    compileOnly 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'

    // 📌 H2 데이터베이스: 가볍고 설치가 필요 없는 인메모리 데이터베이스
    // 개발 단계에서 테스트용으로 매우 유용
    // 실제 운영 환경에서는 MySQL이나 PostgreSQL 등을 사용합니다.
    runtimeOnly 'com.h2database:h2'

    // 📌 테스트 관련 의존성
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

tasks.named('test') {
    useJUnitPlatform()
}